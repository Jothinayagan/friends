<div class="jumbotron mt-3">
  <%= render @friend %>
  <div class='row'>
    <%= link_to "Edit this friend", edit_friend_path(@friend), class: 'btn btn-primary mr-1' %> 
    <%= link_to "Back to friends", friends_path, class: 'btn btn-primary mr-1' %> 
    <%= button_to "Destroy this friend", @friend, method: :delete, class: 'btn btn-danger' %>
  </div>
</div>